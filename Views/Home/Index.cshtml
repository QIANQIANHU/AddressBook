<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <link rel="stylesheet" type="text/css" href="~/css/styles.css" />
    <link rel='stylesheet' href='https://maxcdn.bootstrapcdn.com/bootstrap/3.2.0/css/bootstrap.min.css'>
    <title>Home of address book</title>
  </head>
  <body class="container">
    <h1>Welcome to AddressBook</h1>
    <form action="/CreateForm" method="post">
      <button type='submit' class='btn'>Add address</button>
    </form>
    <h1>Your contact list: </h1>
    <ul>
      <li>Name: @Model.GetUserName()</li>
      <li>Phone number: @Model.GetPhoneNumber()</li>
      <li>address: @Model.GetAddress()</li>
    </ul>

    <!-- <form class="" action="/ContactList" method="post">
      <button type='submit' class='btn'>delete</button>
    </form> -->
  </body>
</html>
